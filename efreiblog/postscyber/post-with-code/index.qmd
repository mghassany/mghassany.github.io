---
title: "Post Cyber"
author: "Boussad"
date: "2023-02-10"
categories: [news, code, analysis]
image: "image.jpg"
---

This is a post with executable code.

## CE 2022-2023

::: {.callout-note icon="false"}
## Exercice 1

Soit $A$ et $B$ deux événements tels que $P(A) = 0.2$ et $P(B) = 0.3$.
Calculer la probabilité que l'un des deux événements $A$ et $B$ se
produit lorsque

1.  $A$ et $B$ sont incompatibles.

2.  $A$ et $B$ sont indépendants.
:::


:::{.callout-important collapse="true" icon="false"}
## Solution (Click to expand)
1.  On a $P(A \cup B)= P(A)+P(B)-P(A\cap B)$ or, puisque $A$ et $B$ sont
    incompatibles,$A\cap B = \emptyset$ et $P(A\cap B)=0$ donc
    $\boxed{P(A \cup B)= P(A)+P(B)=0.5}.$

2.  De même, puisque $A$ et $B$ sont indépendants, $P(A\cap B)=P(A)P(B)$
    donc $\boxed{P(A \cup B)= P(A)+P(B)-P(A)P(B)=0.44}.$
:::

::: {.callout-note icon="false"}
## Exercice 2

Une urne contient une boule qui porte le numéro -1, deux qui portent le
numéro 0 et deux qui portent le numéro 4. On extrait simultanément deux
boules dans cette urne.

1.  Déterminer la loi de probabilité de la variable aléatoire $X$ qui
    représente la somme des nombres obtenus.

2.  Calculer $E(X)$.
:::


:::{.callout-important collapse="true" icon="false"}
## Solution (Click to expand)
1.  Dans cette expérience, l'ensemble des résultats possibles
    (l'univers) est $X(\Omega)=\{-1,0,3,4,8\}$.\
    On a $$\begin{aligned}
            P(X=-1)&=P(-1 \text{ et } 0)=\dfrac{C^1_1 \times C^1_2}{C^2_5}=\dfrac{2}{10}\\[0.3em]
            P(X=0)&=P(0 \text{ et } 0)=\dfrac{C^2_2}{C^2_5}=\dfrac{1}{10}\\[0.3em]
            P(X=3)&=P(-1 \text{ et } 4)=\dfrac{C^1_1\times C^1_2}{C^2_5}=\dfrac{2}{10}\\[0.3em]
            P(X=4)&=P(0 \text{ et } 4)=\dfrac{C^1_2\times C^1_2}{C^2_5}=\dfrac{4}{10}\\[0.3em]
            P(X=8)&=P(4 \text{ et } 4)=\dfrac{C^2_2}{C^2_5}=\dfrac{1}{10}\\[0.3em]
        \end{aligned}$$ Et on vérifie bien que la somme des probabilités
    est égale à 1.

2.  Par définition, on a
    $$\boxed{E(X)=\sum_{x_i} x_i P(X=x_i)=-P(X=-1)+3P(X=3)+4P(X=4)+8P(X=8)=2.8}$$
:::

::: {.callout-note icon="false"}
## Exercice 3

Soit $(X,Y)$ un couple de variables aléatoires dont la probabilité
conjointe est donnée par le tableau suivant:

   *$X$\\$Y$*   *-1*     *0*     *1*
  ------------ ------- ------- -------
      *0*       *1/9*   *2/9*   *1/9*
      *1*       *1/9*   *2/9*   *1/9*
      *2*        *0*    *1/9*    *0*

1.  Calculer les lois marginales de $X$ et $Y$.

2.  Calculer la probabilité que $Y$ soit poisitive ou nulle sachant que
    $X$ vaut 0.

3.  Calculer la covariance de $X$ et $Y$.

4.  $X$ et $Y$ sont elles indépendantes ?
:::


:::{.callout-important collapse="true" icon="false"}
## Solution (Click to expand)
1.  Pour calculer la loi marginale de $X$, on utilise le fait que
    $$\begin{aligned}
            P(X={x_i})&=\sum_{y_i} P(x={x_i}, Y={y_i})\\
            &=P(X={x_i},Y=-1)+ P(X={x_i},Y=0)+P(X={x_i},Y=1)
        \end{aligned}$$

    Cela donne $$\begin{array}{|c|ccc|}
            \hline
            X & 0 & 1 & 2\\
            \hline
            P(X=x) & 4/9 & 4/9 & 1/9\\
            \hline
        \end{array}$$ De même, pour la loi marginale de $Y$, on trouve:
    $$\begin{array}{|c|ccc|}
            \hline
            Y & -1 & 0 & 1\\
            \hline
            P(Y=y) & 2/9 & 5/9 & 2/9\\
            \hline
        \end{array}$$ (et on vérifie que la somme des probabilités est
    égale à 1).

2.  On a $$\begin{aligned}
            P(Y\geq 0 \:\big|\: X=0)
            &=\dfrac{P(Y\geq0 \text{ et } X=0)}{P(X=0)}\\[0.3em]
            &=\dfrac{P(Y=0 \text{ et } X=0)+P(Y=1 \text{ et } X=0)}{P(X=0)}.
        \end{aligned}$$ Donc
    $\boxed{P(Y\geq 0 \:\big|\: X=0)=\dfrac{3}{4}}.$

3.  Par définition, on a $Cov(X,Y)=E(XY)-E(X)E(Y)$, or $$\begin{aligned}
            E(XY)&=\sum_{x_i}\sum_{y_i} {x_i}{y_i}P(X={x_i},Y={y_i})=0,\\
            E(Y)&=-P(Y=-1)+P(Y=1)=0
        \end{aligned}$$ donc $\boxed{Cov(X,Y)=0}.$

4.  **Attention!** $X$ et $Y$ sont indépendantes $\implies Cov(X,Y) = 0$. Ceci est équivalent à $Cov(X,Y)\neq 0 \implies X$ et $Y$ ne sont pas indépendantes.
    La réciproque n'est pas toujours vraie! Nous ne pouvons donc rien conclure de
    la question précédente.\
    Pour savoir si $X$ et $Y$ indépendantes, il faut repartir de la
    définition: $X$ et $Y$ indépendantes si et seulement si pour toutes
    valeurs de $X$ et $Y$ on a $$P(X=x,Y=y)=P(X=x)P(Y=y).$$ Or ici on
    remarque (par exemple) que
    $$P(X=0,Y=-1)=\frac{1}{9}\neq P(X=0)P(Y=-1)=\frac{8}{81}.$$ Donc
    $\boxed{X\text{ et } Y \text{ ne sont pas indépendantes}}$.
:::

::: {.callout-note icon="false"}
## Exercice 4

Une boîte contient trois transistors, que l'on désigne par $A$,$B$ et
$C$. Le transistor $A$ a été fabriqué par une machine qui produit $3\%$
de défectueux, le transistor $B$ par une machine qui produit $5\%$ de
défectueux, et le transistor $C$ par une machine qui produit $7\%$ de
défectueux.

1.  Soit $p$ la probabilité qu'un transistor pris au hasard dans la
    boîte soit défectueux. Montrer que $p=0.05$.

2.  Dix transistors sont pris au hasard (avec remise) dans la boîte.
    Soit $N$ le nombre de transistors défectueux obtenus. Quelle est la
    loi de $N$? (La réponse doit être justifiée et détaillée).

3.  Calculer $P(N=0)$.

4.  Quelle est l'espérance mathématique de $N$.
:::


:::{.callout-important collapse="true" icon="false"}
## Solution (Click to expand)
1.  On note $A,B,C$ les événements "le transistor pris au hasard est le
    transistor $A$" (respectivement, $B$, $C$). On note $D$ l'évenement
    "le transistor est défectueux". $A,B$ et $C$ forment une partition sur $\Omega$. On a $$\begin{aligned}
        P(D) &=P(D \cap A) + P(D  \cap B) + P(D  \cap  C) \text{ (formule de probabilités totales)}\\[0.3em]
            &=P(D\big|A)P(A)+P(D\big|B)P(B)+P(D\big|C)P(C)
        \end{aligned}$$ Cela donne
    $\boxed{p=\dfrac{3+5+7}{300}=\dfrac{5}{100}}$

2.  Nous faisons face à une expérience de Bernouilli (deux résultats
    possibles) avec répétitions indépendantes (car il y a remise). La
    variable aléatoire $N$ compte le nombre de transistors défectueux.
    Elle suit donc une loi binomiale avec $n=10$ et $p=0.05$:
    $$\boxed{N \sim \mathcal{B}(10,0.05)}.$$ (Autrement dit
    $P(N=k)=C^k_n p^k (1-p)^{n-k} \quad for \quad k \in \{0, \ldots , 10\}$.)

3.  Application directe de la formule de la loi binomiale:
    $$\boxed{P(N=0)=0.95^{10} \simeq 0.60}.$$

4.  Nous avons vu dans le cours que, pour une variable $N$ qui suit une
    loi binomiale, on a $\boxed{E(N)=np=0.5}$.
:::

